<!DOCTYPE html>
<html lang="en" data-theme="<%= theme %>">
<%- include('./partials/head') %>

  <body>
    <%- include('./partials/header') %>
    <main>
      <% if (user) { %>
      You are logged in as <%= locals.user.email %>
        <% } else { %>
          <div class="center-menu">
          <div class="subtitle">You are missing out on the fun! Join now!</div>
          <div class="auth-buttons">
            <a href="/register" class="button accent" >Register</a>
            <a href="/login" class="button primary">Login</a>
          </div>
          </div>
           <% } %>
    </main>
    <%- include('./partials/footer') %>

  </body>

</html>